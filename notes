cd Desktop/lead_manager_react_django-master/leadmanager
python3 manage.py runserver
python3 manage.py makemigrations
python3 manage.py migrate

pip3 install virtualenv

virtualenv mypython

#this should be inside LEAD_MANAGER_REACT_DJANGO_MASTER
source mypython/bin/activate




# Install dependencies
npm install

# Serve API on localhost:8000
python leadmanager/manage.py runserver

# Run webpack (from root)
npm run dev

# Build for production
npm run build




delete mydatabase
for updating user and settings related databases
then python3 manage.py migrate

to run shell related to django
python3 manage.py shell


to import all models from leads
from leads.models import *
note we could access it because leads and manage.py are in the same level


la7ez be hay ymken kel shi byn3malo refer lal manage.py
from leads.models import Lead,Marketing,Customers,ProductDevelopment,ProductOperations,HR,ProfitL




for updating my databases, my models
python3 manage.py makemigrations leads
python3 manage.py migrate leads



import { GET_ERRORS } from '../actions/types';

../actions/types m3neta rja3 lwra mra, b3den ra7 ykoon bnfs el level fa befot 3al actions b3den foot 3l types



                <td>{moment(marketing.created_at.toDate()).format('YYYY')}</td>




u i should replace addMarketing with addLead to make it work in marketing.js

there is a functionality that can be implemented in messages.js in actions










'../actions/types.js'   rja3 3al folder li ana bi albo w men honek roo7






to start a new project go to LEAD_MANAGER_REACT_DJANGO_MASTER FOR EAMPLE:

django-admin startproject leadmanager
python manage.py startapp leads




To create superuser for admin for managemnet purposes
python manage.py createsuperuser





to extract requirements
pip3 freeze > requirements.txt



#to install requirements
pip3 install -r requirements.txt




lsteps l3mlton lal git


inside LEAD_MANAGAR_REACT_DJANGO-master



git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Ahmad43211/Manager.git
git push -u origin main


but note i need to find about the global config



cycle of working with git


git pull
git status
git add .
git commit -m "second commit"
git status

git push


git reset from undoing git add .

git reset something for undoing something

git log show the history of commits with commit name the most upper one is the latest, i need to check the code meaning 

now am inside the origin/main

To delete git 
rm -rf .git


#steps to create new branch

git branch alpha
git status
#to get all branches
git branch --list
#to change to the other branch
git checkout alpha
#to get the branches we have
git branch
#to create a branch and checkout to it in one step
git checkout -b beta
#for the first time when we create a new branch and checkout to it, we need to push using the following command or create it in github and push normally
git push --set-upstream origin alpha

#note:when he checkout to the master the editted files in the alpha in the code editor disappeared







#to create a file
touch style.css

#to check the files inside this director
ls -la
